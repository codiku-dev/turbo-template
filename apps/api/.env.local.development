# API configuration
PORT=3090

# PostgreSQL Docker configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=turbo_template
POSTGRES_PORT=5432

# Database URL (must use actual values, not variables)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/turbo_template?schema=public

TRPC_URL=http://localhost:3090/trpc
